name: ciro_app
description: "Crisis Intelligence & Response Orchestrator (CIRO) Flutter Client"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Required dependencies specified by user
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  flutter_local_notifications: ^16.1.0
  fl_chart: ^0.65.0
  google_maps_flutter: ^2.5.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
